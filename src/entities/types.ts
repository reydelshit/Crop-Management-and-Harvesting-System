export type UserType = {
  user_id: string
  account_type: string
  created_at: string
  username: string
}

export type CropTypes = {
  crops_id: string
  crops_name: string
  expected_harvest: string
  ogc: string
  planting_date: string
  crops_img: string
  variety: string
}
